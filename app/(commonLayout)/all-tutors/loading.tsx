import CustomLoader from '@/components/pageComponents/loader';
import React from 'react';

const loading = () => {
    return (
        <CustomLoader></CustomLoader>
    );
};

export default loading;